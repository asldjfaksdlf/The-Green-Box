import React from "react";
import ReactDOM from "react-dom/client";
import GreenBoxApp from "./GreenBox.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GreenBoxApp />
  </React.StrictMode>
);
